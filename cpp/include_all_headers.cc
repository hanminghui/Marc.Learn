/* include all c++ standard library headers
 * only used for learning and test
 * not portable, gcc only
 */
#include <bits/stdc++.h>

int main(int argc, char *argv[])
{
	std::string str("test");
    std::cout << str << std::endl;
    return 0;
}
